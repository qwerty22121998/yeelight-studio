//! Ambient screen-capture light: capture a screen region and stream its color to a bulb.

// Items in this module are consumed by tasks not yet implemented; suppress dead-code noise.
#![allow(dead_code)]

pub(crate) mod color;
